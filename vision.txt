The images below show the ACTUAL current design photographed from multiple angles in a CAD application. Study the exact geometry, proportions, structural members, and connection points carefully before writing the image prompt.

Your task: write a single image generation prompt (no preamble, no explanation — just the prompt) that describes this design WITH the requested modification visually applied.

The prompt must produce an image that:
- Looks like a clean engineering concept sketch or technical line drawing
- Uses fine black ink or pencil lines on a pure white background
- Shows the object from a 3/4 isometric perspective, matching the camera angle of the CAD views
- Accurately reflects the actual geometry of the CURRENT design (number of beams, holes, connector positions, gear teeth count, axle lengths, etc.) then clearly illustrates the modification
- Includes light cross-hatching or subtle shading only where needed to convey depth or material
- Has NO text, NO labels, NO dimensions, NO background objects, NO colour fills — only lines and minimal shading
- Feels hand-drafted but precise: the aesthetic of a mechanical engineering design notebook

Output the image prompt only.
